# Decision log

## 2026-09-08 — 独立建仓，不合并三条上游

### Decision

晨报单独一个仓库；AI 日报、财经日报、K 线日报三个上游仓库保持独立，各自只承认
`contracts/` 里的交付合同。

### Why

三条管道是三个领域：内容抓取与摘要、新闻评分与事件聚合、跨资产 K 线分析。它们有
各自的测试、运行时和失败模式，其中 K 线日报还和 A 股投委会共用一个千级 PR 的大仓。
合并需要数月搬迁，而读者看到的东西一点不会变——产品层面的合并已经由这一页完成了。

晨报之前住在网站仓库里，有三个问题：它是一个有自己节奏和设计的产品；每天往网站仓库
提交约 700KB 的 HTML；三条上游怎么失败、怎么重跑的知识没有落处。

### Gotchas

- 搬迁必须证明产物不变。方法是同一天输入、两边构建、逐字节比对，只归一化生成时间戳。
- 拆模块后，测试要能 patch 路径：所有模块通过 `config.X` 引用可变路径，而不是
  `from .config import X`，否则 patch 不生效。
- `cli.py` 里局部变量 `llm` 与模块同名，模块须 `import as llm_mod`。

## 2026-09-08 — Codex CLI 作为默认模型

### Decision

默认 `--llm codex`；DeepSeek 需要显式指定。任一 provider 因余额或缺失失败，
标记为本轮不可用，不再逐条重试；全部不可用时个股描述退回规则生成。

### Why

DeepSeek 账户余额为 0，三条上游当天全部触发 402。晨报不能因为一个账户余额而失效。

### Gotchas

- 从 Python 子进程调 `codex exec` 必须 `stdin=subprocess.DEVNULL`，否则无限等待。
- cwd 必须是空临时目录；在仓库里跑会加载项目上下文，单次调用能卡几分钟。
- `pkill -f "codex exec"` 会误杀其它会话的 Codex，只能按 PID 杀。

## 2026-09-08 — 三栏阅读版式，缺失周期不放占位

### Decision

桌面端三栏各自独立滚动；没有数据源的周期直接不显示；每只个股都带一张迷你 K 线，
不再折叠。

### Why

Park 的原话：视觉太重、看着累，占位是运维思维，个股只有一句话看不出东西。

### Gotchas

- 宏观资产关键词映射有先后顺序，「中证红利」必须排在「红利」前面，否则覆盖 SCHD。
- 图表懒加载的 IntersectionObserver 必须以 K 线栏为 root，因为页面本身不滚动。
